nodejs:
  build: .
  environment:
    -NODE_PORT: 8888
    -TWIT_PRI_KEY: 1234
    -TWIT_PUB_KEY: 5678
nginx:
  build: resources/nginx
  volumes:
    - /vagrant/logs/nginx:/var/log/nginx
    - /vagrant/web:/srv/http
  ports:
    - "80:80"
  links:
    - nodejs:node